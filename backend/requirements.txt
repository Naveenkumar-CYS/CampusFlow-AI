fastapi==0.141.1
uvicorn[standard]==0.34.0
sqlalchemy==2.0.52
psycopg2-binary==2.9.10
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0
pytest==8.3.4
httpx==0.28.1
pyjwt==2.10.1
bcrypt==4.2.1

# Redis Streams event bus (Stage 1: event bus foundation)
redis==5.2.1

# AI/ML foundation (Stage 5A: student risk-classification model).
# scikit-learn is the classifier; joblib (a scikit-learn dependency, but
# pinned directly since app/analytics/model.py imports it explicitly)
# persists the trained artifact to disk.
scikit-learn==1.8.0
joblib==1.5.3
